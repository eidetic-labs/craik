[build-system]
requires = ["hatchling==1.26.3"]
build-backend = "hatchling.build"

[project]
name = "craik"
version = "0.12.8"
description = "Governed agent-runtime substrate for case files, policy, receipts, prompt compilation, and provider transports."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
  { name = "Eidetic Labs" }
]
keywords = [
  "agents",
  "ai",
  "automation",
  "memory",
  "runtime"
]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development",
]
dependencies = [
  "click==8.1.8",
  "google-auth>=2.30",
  "pydantic==2.13.4",
  "requests>=2.32",
  "textual>=0.85",
  "typer==0.12.5",
]

[project.optional-dependencies]
dev = [
  "hypothesis==6.148.0",
  "mypy==1.14.1",
  "pytest==9.0.3",
  "pytest-cov==6.0.0",
  "ruff==0.8.4",
  "vulture>=2.11",
]

[project.scripts]
craik = "craik.cli:main"

[project.urls]
Homepage = "https://github.com/eidetic-labs/craik"
Issues = "https://github.com/eidetic-labs/craik/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/craik"]
artifacts = ["src/craik/py.typed"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
markers = [
  "integration: recorded or optional live provider integration tests",
  "live: tests that require an explicitly enabled live local provider",
]

[tool.coverage.run]
branch = true
source = ["craik"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = true

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["craik"]

[tool.vulture]
paths = ["src/craik"]
min_confidence = 80
ignore_decorators = ["@*.command", "@*.callback", "@pytest.fixture"]
